import { render, screen } from '@testing-library/react';
import App from '../App';
import Index from '../index';

test('test render app function', () => {
  render(<App />);
  
});

test('test render index function', () => {
  //render(<Index />);
});