language: node_js
node_js:
  - "14"
cache:
  directories:
    - node_modules
jobs:
  include:
    - stage: Lint, FrontendTest, BackendTest, Build
      name: Lint
      script:
        - npm install
        - npm run lint
    - name: FrontendTest
      install: travis_wait 20 mvn install
      script:
        - npm install
        - npm run test
    - name: BackendTest
      script:
        - npm install
        - cd server
        - npm install
        - npm run test
    - name: Build
      script:
        - npm run build
branches:
  only:
    - master
    - tests
